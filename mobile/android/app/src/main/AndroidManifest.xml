<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ═══════════════════════════════════════════════════════════════════
         BU DOSYADA HİÇBİR İZİN YOK — VE BU, ÜRÜNÜN EN GÜÇLÜ KANITI

         Burada iki izin vardı:

           CAMERA   — QR ile kişi ekleme akışı içindi. O akış 24 Ağustos'ta
                      devralınan mesajlaşma arayüzüyle birlikte silindi;
                      izin öksüz kaldı.
           INTERNET — avatar görsellerini indiren cached_network_image
                      içindi. O paket de aynı gün kaldırıldı. Geriye kalan
                      tek ağ tüketicisi google_fonts'tu (yazı tiplerini
                      çalışma zamanında indiriyordu) ve o da 9 Eylül'de
                      kaldırıldı.

         Bugün uygulama çalışma zamanında tek bir ağ çağrısı yapmıyor.
         Dolayısıyla INTERNET iznini İSTEMİYOR.

         Bunun önemi şu: "metin cihazdan çıkmaz" iddiası artık bize
         inanmayı gerektirmiyor. Kullanıcı telefonun Ayarlar > Uygulamalar
         ekranından izinlere bakar ve görür — bu uygulamanın internete
         çıkma yetkisi yok. Bir mahremiyet iddiasının işletim sistemi
         tarafından doğrulanabilir olması, gizlilik politikası yazmaktan
         başka bir şeydir.

         Hata ayıklama ve profil sürümleri INTERNET iznini ayrı manifest
         dosyalarından alır (src/debug, src/profile); Flutter'ın sıcak
         sürümü bu dosyadan üretilir ve izinsizdir.

         13 Eylül 2026: INTERNET, VIBRATE ve usesCleartextTraffic="true"
         bir ara commit'le (9a3f7b8) buraya geri girmişti — klavye servisine
         eklenen, metni şifresiz HTTP ile sunucuya gönderen yollar içindi.
         Yollar ve izinler kaldırıldı. Klavyenin titreşimi
         `performHapticFeedback` ile yapılır ve izin gerektirmez. Bu yorumun
         yeniden yalana dönmemesi için `kapsam_degismezi_test.dart` artık bu
         dosyayı okur.
         ═══════════════════════════════════════════════════════════════════ -->

    <!-- allowBackup="false" (docs/24 · madde 42): uygulama verisi Google
         yedeğine ve `adb backup` ile dışarı alınmaz. Uygulama bugün kalıcı
         veri yazmıyor; bu satır, ileride yazılacak her şeyin (ayarlar,
         topluluk sayıları) varsayılan olarak cihazda kalmasını güvenceye
         alır. `kapsam_degismezi_test.dart` denetler. -->
    <application
        android:label="Üslup"
        android:name="${applicationName}"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <service
            android:name=".CivilityInputMethodService"
            android:exported="true"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:label="Üslup Klavyesi">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
