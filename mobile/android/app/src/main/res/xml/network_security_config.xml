<?xml version="1.0" encoding="utf-8"?>
<!--
  Ağ güvenliği politikası — release dahil tüm derlemeler.

  Varsayılan: şifresiz HTTP YASAK. Ürünün temel iddiası mahremiyettir;
  buluta giden tek akışın düz metin taşımasına izin verilmez.

  Tek istisna, geliştirme/demo sunucusunun çalıştığı yerel adreslerdir:
  10.0.2.2 Android emülatöründe ana makinenin localhost'udur. Bu adresler
  cihazdan dışarı çıkmaz.

  Gerçek dağıtımda sunucu HTTPS ile sunulmalı ve buradaki istisna
  tamamen kaldırılmalıdır.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
